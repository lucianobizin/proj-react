/*const getProducts = () => {

    return new Promise((resolve) => {
        resolve(products);
    }).catch(error => {
        console.log(error);
    });
}

const getProductByCategory = (idCategory) => {
    return new Promise(resolve => {
        setTimeout(() => {
            const productCategory = products.filter(prod => prod.category === idCategory)
            resolve(productCategory);
        }, 100)
    })
}

const getOneProduct = (id) => {
        return new Promise(resolve => {
            const product = products.find(prod => prod.id === id);
                resolve(product);
        })
    }
*/